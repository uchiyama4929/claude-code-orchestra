name = "fable-advisor"
description = "Read-only escalation coordinator for arbitration, stuck work, and final large-change review."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/agents/fable-advisor.md` before starting and preserve its
read-only boundary. When an actual Fable opinion is required and Claude CLI is
available, invoke it through `.agents/skills/_shared/cli_consult.py --cli claude
--model claude-fable-5[1m]` with a prompt file and no write access. If that CLI
is unavailable, provide a Codex fallback and disclose that Fable was not run.
"""
